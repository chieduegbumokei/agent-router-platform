# Backend API base URL (Express locally; API Gateway + Function URL deployed)
NEXT_PUBLIC_API_URL=http://localhost:4000
# When deployed, the streaming chat endpoint may live on a separate Function URL:
# NEXT_PUBLIC_CHAT_URL=https://xxxx.lambda-url.us-east-1.on.aws
